# Nix CI - Parallel validation workflow
#
# Jobs run in parallel where possible:
# - lint: flake.lock health (no Nix needed)
# - validate: flake structure check
# - format: Nix code formatting
# - build: Darwin build + symlink verification (cached)
#
# Cache: Magic Nix Cache saves on main push, PRs use upstream cache.nixos.org.

name: Nix

on:
  push:
    branches: [main]
    paths:
      - '**.nix'
      - 'flake.lock'
  pull_request:
    paths:
      - '**.nix'
      - 'flake.lock'
      - 'modules/**'
      - 'scripts/**'

jobs:
  lint:
    name: Lint flake.lock
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Check flake.lock health
        uses: DeterminateSystems/flake-checker-action@main
        with:
          nixpkgs-keys: nixpkgs

  validate:
    name: Validate flake
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Nix
        uses: DeterminateSystems/determinate-nix-action@v3
        with:
          flakehub: false

      - name: Check flake structure
        run: nix flake check --no-build

  format:
    name: Check formatting
    runs-on: macos-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Nix
        uses: DeterminateSystems/determinate-nix-action@v3
        with:
          flakehub: false

      - name: Check Nix formatting
        run: nix-shell -p nixfmt-classic --run 'find . -name "*.nix" -exec nixfmt --check {} +'
        continue-on-error: true

  build:
    name: Build & Verify
    runs-on: macos-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout repository
        uses: actions/checkout@v6

      - name: Install Nix
        uses: DeterminateSystems/determinate-nix-action@v3
        with:
          flakehub: false

      - name: Setup Magic Nix Cache
        if: github.ref == 'refs/heads/main'
        uses: DeterminateSystems/magic-nix-cache-action@main

      - name: Build home-manager configuration
        run: nix build .#ci.hmActivationPackage -o result-hm

      - name: Verify symlinks
        run: ./scripts/verify-symlinks.sh result-hm/home-files
