# Reusable: Nix Build (macOS)
# Called by ci-gate.yml and ci-nix.yml
name: _nix-build

on:
  workflow_call:

# Performance: Cancel in-progress builds when new commits arrive
concurrency:
  group: nix-build-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  build:
    name: Build
    runs-on: macos-latest
    permissions:
      contents: read
      id-token: write
    steps:
      - name: Checkout
        uses: actions/checkout@v6

      # Performance: Install Nix with CI-optimized settings
      - name: Install Nix
        uses: DeterminateSystems/determinate-nix-action@v3
        with:
          extra-conf: |
            max-jobs = auto
            cores = 0
            http-connections = 50
            connect-timeout = 5
            stalled-download-timeout = 10
            narinfo-cache-positive-ttl = 86400
            fallback = true

      # Caching: Free, zero-config Nix store cache via GitHub Actions cache
      # DO NOT replace with flakehub-cache-action (PAID) or raw actions/cache (breaks on /nix/store)
      - name: Cache Nix Store
        uses: DeterminateSystems/magic-nix-cache-action@v13

      - name: Start Build Timer
        id: build-timer
        run: echo "start=$(date +%s)" >> "$GITHUB_OUTPUT"

      - name: Build Home Manager
        run: ./scripts/workflows/build-hm.sh result-hm

      - name: Verify Nix Symlinks
        run: ./scripts/workflows/verify-symlinks.sh result-hm/home-files

      - name: Build Timing
        if: always()
        run: |
          start="${{ steps.build-timer.outputs.start }}"
          if [ -n "$start" ]; then
            elapsed=$(( $(date +%s) - start ))
            echo "::notice::Build + verify completed in ${elapsed}s"
          fi
