# NixOS version tracking
# This file tracks the stable NixOS version used by this configuration
# Used by CI to verify the version hasn't reached end-of-life
{
  # Current stable NixOS version
  # Update this when upgrading to a new stable release
  stableVersion = "25.05";
}
