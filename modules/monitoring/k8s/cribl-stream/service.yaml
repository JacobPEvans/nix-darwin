apiVersion: v1
kind: Service
metadata:
  name: cribl-stream
  namespace: monitoring
  labels:
    app: cribl-stream
spec:
  type: ClusterIP
  selector:
    app: cribl-stream
  ports:
    - name: api
      port: 9000
      targetPort: 9000
      protocol: TCP
    - name: inputs
      port: 10080
      targetPort: 10080
      protocol: TCP
    - name: leader
      port: 4200
      targetPort: 4200
      protocol: TCP
